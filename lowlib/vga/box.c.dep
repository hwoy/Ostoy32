#include "vga.h"
#include "asm.h"


void box(unsigned int x1,unsigned int y1,unsigned int x2,unsigned int y2,unsigned short bg,unsigned short fg)
{
  unsigned int i,j;
  
    for(j=y1;j<=y2;j++)
      {
	for(i=x1;i<=x2;i++)
	  {
		vidmem[i+j*COL]=(vidmem[i+j*COL]&0x8fff)|(bg<<12);
	    vidmem[i+j*COL]=(vidmem[i+j*COL]&0xf0ff)|(fg<<8) ;
	    vidmem[i+j*COL]=vidmem[i+j*COL]&0xff00;
	  }
      }
}

